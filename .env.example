# Optional live completed-solds API. When unset, Comps uses the honest stub
# (no invented prices). The endpoint must return only completed solds.
# EXPO_PUBLIC_COMPS_API_URL=https://example.invalid/comps
