# Optional live completed-solds API. When unset, Comps uses SAMPLE stub rows
# (example.invalid source URLs, labeled not live). Live payloads should include
# sourceUrl and optional sourceLabel on each completed sold.
# EXPO_PUBLIC_COMPS_API_URL=https://example.invalid/comps
